<?php

    # if for some reason you don't want certain fields
    # showing up in your forms for certain tables, put them here

    $fields2omit_by_table = array(
        #'movie' => array(
        #    'tags'
        #),
    );
